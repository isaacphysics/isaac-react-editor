module.exports = {
    webpack: {
      configure: (webpackConfig, { env, paths }) => {
        console.log("TODO MT - Add a loader for .nearley files");
        return webpackConfig;
      },
    },
  };